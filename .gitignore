# IDE
.DS_Store
Thumbs.db
*.log
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Backend (Laravel)
backend/.env
backend/.env.backup
backend/.env.production
# .env.e2e : genere localement depuis .env.e2e.example (APP_KEY creee au
# premier run des tests E2E) — jamais versionne, une cle fonctionnelle
# n'a rien a faire dans git.
backend/.env.e2e
# .env.jury : variante de .env.docker avec identifiants SMTP de demonstration,
# injectee uniquement dans le zip remis au jury — jamais versionnee.
backend/.env.jury
# backend/.env.docker : versionne volontairement — template de demo Docker,
# sans secret reel (mots de passe admin et BDD injectes via docker-compose).
backend/.phpactor.json
backend/.phpunit.result.cache
backend/.phpunit.cache
backend/auth.json
backend/node_modules
backend/public/build
backend/public/css
backend/public/hot
backend/public/js
backend/public/storage
backend/public/vendor
backend/storage/*.key
backend/storage/pail
backend/vendor
Homestead.json
Homestead.yaml

# Frontend (Vue.js)
frontend/node_modules
frontend/dist
frontend/.env.local
frontend/.env.*.local

# Rapports de couverture de tests (generes a la demande)
backend/coverage-html
frontend/coverage

# Playwright E2E (rapports + storageState authentifie, jamais versionnes)
frontend/playwright-report/
frontend/test-results/
frontend/e2e/.auth/

# Claude
.claude/settings.local.json

# Notes locales
docs/notes/

# Tooling / scratch local
.claude/
audit-curseur.md
